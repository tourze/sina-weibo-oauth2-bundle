# Test routes for EasyAdmin tests

# Define app_logout route required by EasyAdmin templates
app_logout:
  path: /logout
  methods: [POST]

# Define app_login route for security
app_login:
  path: /login
  methods: [GET, POST]

# EasyAdmin routes
easyadmin:
  resource: .
  type: 'easyadmin.routes'